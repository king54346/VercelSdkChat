# 快速开始指南

## 📋 前置要求

- Node.js >= 18
- npm 或 yarn
- OpenAI API Key

## 🚀 5 分钟快速启动

### 步骤 1: 克隆并安装

```bash
# 如果还没有安装依赖
npm install
```

### 步骤 2: 配置环境变量

创建 `.env` 文件:

```bash
# Windows
copy .env.example .env

# macOS/Linux
cp .env.example .env
```

编辑 `.env` 并添加你的 OpenAI API Key:

```env
OPENAI_API_KEY=sk-your-api-key-here
```

### 步骤 3: 启动应用

```bash
# 同时启动前端和后端
npm run dev:all
```

或者分别启动:

```bash
# 终端 1: 启动前端
npm run dev

# 终端 2: 启动后端
npm run server
```

### 步骤 4: 打开浏览器

访问 http://localhost:5173

## 🎯 快速测试

打开应用后,尝试以下对话:

### 示例 1: 代码分析
```
帮我分析这段代码:

function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
}
```

### 示例 2: 生成测试
```
为这个函数生成测试:

export function add(a: number, b: number) {
  return a + b;
}
```

### 示例 3: 性能优化
```
这段代码有性能问题吗?

const result = [];
for (let i = 0; i < 1000; i++) {
  for (let j = 0; j < 1000; j++) {
    result.push(i * j);
  }
}
```

## 🔧 可用工具

### MCP 工具 (可扩展)
- `readFile` - 读取文件
- `searchCode` - 搜索代码
- `listDirectory` - 列出目录
- `getFileInfo` - 文件信息

### Skills (内置功能)
- `analyzeCode` - 代码分析
- `refactorCode` - 重构建议
- `generateDocumentation` - 生成文档
- `generateTests` - 生成测试
- `explainCode` - 代码解释
- `findBugs` - 查找 Bug
- `optimizePerformance` - 性能优化

## 💡 提示

1. **多步骤任务**: Agent 可以自动链接多个工具来完成复杂任务
2. **上下文保持**: 对话会保持上下文,可以进行多轮交互
3. **工具选择**: AI 会根据你的问题自动选择最合适的工具

## ❓ 常见问题

### Q: 如何获取 OpenAI API Key?
A: 访问 https://platform.openai.com/api-keys 创建一个新的 API Key

### Q: 可以使用其他 AI 模型吗?
A: 可以!编辑 `src/server.ts` 中的模型配置:
```typescript
model: openai('gpt-3.5-turbo')  // 改为其他模型
```

### Q: 如何添加自定义工具?
A: 编辑 `src/lib/mcp-tools.ts` 或 `src/lib/skills.ts` 添加新的工具定义

### Q: 后端 API 端口被占用?
A: 修改 `src/server.ts` 中的 `PORT` 变量,或设置环境变量 `PORT=3002`

## 📚 下一步

- 查看 [USAGE.md](./USAGE.md) 了解详细使用指南
- 查看 [src/lib/mcp-config.ts](./src/lib/mcp-config.ts) 了解如何配置实际的 MCP 服务器
- 自定义和扩展工具来满足你的需求

## 🆘 需要帮助?

如果遇到问题:
1. 检查终端是否有错误信息
2. 确认 `.env` 文件配置正确
3. 确保后端服务正在运行
4. 查看浏览器控制台是否有错误
